# Common configuration for all languages
disable-default-queries: false

# C++ specific configuration
cpp:
  query-filters:
    - exclude:
        id:
          - "cpp/missing-return"
          - "cpp/offset-of-non-standard"
  paths-ignore:
    - "test/**"
    - "deps/**"
    - "benchmark/**"
